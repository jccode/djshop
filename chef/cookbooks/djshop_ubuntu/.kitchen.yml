---
driver:
  name: vagrant
  network:
    - ["private_network", {ip: "192.168.34.10"}]

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - recipe[djshop_ubuntu::default]
    attributes:
